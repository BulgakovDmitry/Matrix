#include "driver.hpp"
#include "dump.hpp"
#include "matrix/matrix.hpp"
#include <iostream>

int main() {
    driver();
    // matrix::Matrix<double> a(2);
    // matrix::Matrix<double> b(2);

    // a[0][0] = 1;   a[0][1] = 2;
    // a[1][0] = 3;   a[1][1] = 4;

    // b[0][0] = 5;   b[0][1] = 6;
    // b[1][0] = 7;   b[1][1] = 8;

    // std::cout << a << " + " << b << " = " << a + b;
}